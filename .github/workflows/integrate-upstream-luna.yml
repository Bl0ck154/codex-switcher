name: Integrate upstream and Luna Reserve

on:
  push:
    branches:
      - merge/upstream-0.2.15-luna-reserve

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: echo integration-workflow-ready
